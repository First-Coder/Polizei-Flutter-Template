services:
  flutter-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flutter-web
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - test_data:/data

volumes:
  test_data: